# 14. Find Non-Repeating Elements in an Array

## 📌 Problem Statement

Write a program to find all **non-repeating elements** in a given array.

---

## 📝 Input Format

* A single line containing space-separated integers (array elements)

---

## 📤 Output Format

* Print all elements that appear exactly once
* If no such element exists, print `No Non-Repeating Elements`

---

## 💡 Approach (Explanation)

1. Read the input array
2. Use a `HashMap` to store frequency of each element
3. Traverse the array:

   * Update frequency using `getOrDefault()`
4. Traverse the map:

   * If frequency == 1 → print the element
5. If none found → print message

---

## 💻 Java Code

```java id="non-repeating-elements"
import java.util.*;

class Main {
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);

        String[] input = sc.nextLine().split("\\s+");
        int[] arr = new int[input.length];

        for(int i = 0; i < input.length; i++){
            arr[i] = Integer.parseInt(input[i]);
        }

        HashMap<Integer, Integer> map = new HashMap<>();

        // Count frequency
        for(int num : arr){
            map.put(num, map.getOrDefault(num, 0) + 1);
        }

        boolean found = false;

        // Print non-repeating elements
        for(Map.Entry<Integer, Integer> entry : map.entrySet()){
            if(entry.getValue() == 1){
                System.out.print(entry.getKey() + " ");
                found = true;
            }
        }

        if(!found){
            System.out.println("No Non-Repeating Elements");
        }
    }
}
```

---

## 🧪 Sample Input

```text id="input1"
1 2 2 3 1 4 5
```

---

## ✅ Sample Output

```text id="output1"
3 4 5
```

---

## 📊 Complexity Analysis

* Time Complexity: **O(n)**
* Space Complexity: **O(n)**

---

## 🔥 Key Points

* Frequency count helps identify unique elements
* Elements with frequency = 1 are non-repeating
* Efficient and commonly used pattern

---
